# eating-medication 代码审查报告

**审查日期：** 2026-09-14  
**仓库：** [diaoyunxi/eating-medication](https://github.com/diaoyunxi/eating-medication)  
**审查范围：** 全仓 Python 源码（server / family_monitor / elderly_assistant / common / tests / updater.py）  
**工具：** ruff v0.16.7 + 人工审查  
**代码行数：** 28,555 行（不含 .git / .venv）

---

## 一、仓库概况

本项目是一个**老人用药管理智能助手**，采用三端架构：

| 模块 | 说明 | 技术栈 |
|------|------|--------|
| `server/` | 后端 API 服务 | FastAPI + SQLAlchemy + Alembic |
| `family_monitor/` | 子女端 Web 前端 | Flask + Jinja2 |
| `elderly_assistant/` | 老人端（行空板硬件） | GUI + 硬件驱动 |
| `common/` | 跨端共享工具包 | 纯标准库 |
| `tests/` | 测试套件 | pytest + unittest |
| `updater.py` | 自动更新模块 | 标准库 + gh CLI |

项目整体架构清晰，文档详尽（中文注释覆盖率高），已有 85 个 PR 记录。安全措施（JWT、bcrypt、CSP、限流、请求体限制、Release Attestation 验证等）已较为完善。

---

## 二、审查维度与结论

### 2.1 代码质量

ruff 扫描发现 **1,444 个问题**，按类型分布：

| 规则 | 数量 | 说明 | 严重程度 |
|------|------|------|----------|
| BLE001 | 331 | 裸 `except Exception` 捕获 | 中 |
| UP045 | 171 | 应使用 `X \| None` 替代 `Optional[X]` | 低 |
| EXE002 | 154 | 可执行文件缺少 shebang | 低 |
| UP009 | 149 | 不必要的 UTF-8 编码声明 | 低 |
| I001 | 123 | 导入排序不规范 | 低 |
| B008 | 115 | FastAPI Depends 中的可变默认参数 | 中 |
| UP006 | 77 | 应使用 `list` 替代 `List` | 低 |
| S110 | 62 | try-except-pass 未记录异常 | 低 |
| F401 | 52 | 未使用的导入 | 低 |
| PLW1510 | 27 | subprocess.run 缺少 check 参数 | 中 |
| DTZ005/DTZ001 | 33 | 日期时间未使用时区信息 | 中 |
| F541 | 13 | 无占位符的 f-string | 低 |
| TRY002 | 7 | 应创建自定义异常类 | 中 |
| 其他 | ~180 | 各类小问题 | 低 |

> 注：大量低严重度问题（UP045、UP009、I001、UP006、EXE002、F401、F541 等）已在现有 open PR #63-#82 中覆盖。

### 2.2 安全性

**已有的安全措施（良好）：**
- JWT 认证 + bcrypt 密码哈希（12 rounds）
- 防时序攻击的 dummy hash 验证
- Content-Security-Policy、X-Frame-Options 等安全响应头
- IP 速率限制（登录/注册等敏感路径）
- 请求体大小限制（1MB）
- 敏感字段日志脱敏
- Release Attestation 验证（自动更新）
- 生产环境 SECRET_KEY 强校验

**现有 open PR 已覆盖的安全问题（良好）：**
- #61: Turnstile 人机验证静默放行
- #60: OAuth JWT 安全改进
- #59: 邮箱验证码安全随机源
- #58: TOTP 爆破防护
- #57: X-Forwarded-For 伪造防护
- #56: 设备绑定安全校验
- #55: 更新端点鉴权
- #54: OAuth 邮箱合并绑定安全

### 2.3 潜在缺陷

- **裸异常捕获泛滥（BLE001 × 331）：** 大量 `except Exception` 会吞掉 `KeyboardInterrupt`/`SystemExit` 以外的所有异常，掩盖真正的编程错误。PR #83 仅覆盖安全关键路径，其余 300+ 处未处理。
- **datetime 未绑定时区（DTZ001/DTZ005 × 33）：** 多处使用 `datetime.now()` / `datetime()` 未传 `tzinfo`，在跨时区部署时可能产生时间偏差。
- **try-except-pass 静默吞异常（S110 × 62）：** 异常被完全忽略，不记录日志，排障困难。

### 2.4 项目结构

项目结构良好，三端分离清晰，`common/` 共享包避免了代码重复。`server/` 内部按 `api/core/models/schemas/services/middleware/tasks/utils/websocket` 标准分层。

### 2.5 文档完整性

- README.md 详尽（含部署指南、配置说明）
- 中文注释覆盖率极高（几乎每个函数都有 docstring）
- SECURITY_AUDIT_ERROR_LEAK.md 记录了安全审计历史
- PRIVACY.md / TERMS.md 合规文件齐全
- deploy/ 目录含部署脚本和 Cloudflare 隧道配置
- VERSION 文件管理版本号

---

## 三、按严重程度分级的问题列表

### 严重（Critical） — 已有 open PR 覆盖

| 编号 | 问题 | 已有 PR |
|------|------|---------|
| C-01 | Turnstile 人机验证未配置时静默放行所有请求 | #61 |
| C-02 | AIService 可变类属性跨实例共享 | #62 |
| C-03 | OAuth 绑定 JWT 暴露在 URL 中 | #60 |
| C-04 | 更新端点缺少鉴权 | #55 |
| C-05 | 邮箱验证码使用非密码学安全随机源 | #59 |

### 高（High） — 已有 open PR 覆盖

| 编号 | 问题 | 已有 PR |
|------|------|---------|
| H-01 | X-Forwarded-For 可被伪造绕过限流 | #57 |
| H-02 | TOTP 动态码端点缺少按令牌锁定 | #58 |
| H-03 | 设备绑定缺少所有权校验 | #56 |
| H-04 | OAuth 合并绑定未验证邮箱所有权 | #54 |
| H-05 | 未使用导入 52 处 | #63 |
| H-06 | subprocess.run 缺少 check 参数 27 处 | #65 / #81 |

### 中（Medium） — 新发现，尚无 PR 覆盖

| 编号 | 问题 | 规则 | 数量 | 影响 |
|------|------|------|------|------|
| M-01 | datetime.now() 未绑定时区 | DTZ005 | 22 | 跨时区部署时间偏差 |
| M-02 | datetime() 构造未传 tzinfo | DTZ001 | 11 | 同上 |
| M-03 | 直接 raise Exception 而非自定义异常 | TRY002 | 7 | 异常处理粗粒度，无法区分错误类型 |
| M-04 | 嵌套 with 语句可合并 | SIM117 | 4 | 代码可读性 |
| M-05 | 裸异常捕获在安全关键路径之外仍有 300+ 处 | BLE001 | ~300 | 掩盖编程错误 |
| M-06 | try-except-pass 未记录异常 | S110 | 62 | 排障困难 |

### 低（Low） — 已有 open PR 覆盖

| 编号 | 问题 | 已有 PR |
|------|------|---------|
| L-01 | 不必要的 UTF-8 编码声明 149 处 | #66 |
| L-02 | 导入排序不规范 123 处 | #68 |
| L-03 | 现代化类型注解 270+ 处 | #67 |
| L-04 | 无占位符 f-string 13 处 | #64 |
| L-05 | 可执行文件缺少 shebang 154 处 | #69 / #73 |
| L-06 | 重复导入 4 处 | #70 |
| L-07 | 未使用局部变量 8 处 | #71 |
| L-08 | 未使用的解包变量 17 处 | #72 |

---

## 四、改进建议

### 短期（1-2 周）

1. **合并已有的 30 个 open PR**，特别是安全类（#54-#62）和代码质量类（#63-#82），消除 1,200+ 个 ruff 告警。
2. **为 datetime 调用统一绑定时区**（M-01/M-02）：全仓使用 `datetime.now(timezone.utc)` 替代 `datetime.now()`，构造 datetime 时始终传 `tzinfo`。
3. **为 vision_service 创建自定义异常**（M-03）：将 `raise Exception(...)` 改为 `class OCRError(Exception)`，使调用方可精确捕获 OCR 相关错误。

### 中期（1-2 月）

4. **逐步收窄 BLE001**（M-05）：PR #83 覆盖安全关键路径后，对剩余 300+ 处按模块逐步收窄为具体异常类型（如 `OSError`、`ValueError`、`json.JSONDecodeError`）。
5. **为 S110 添加日志记录**（M-06）：将 `except Exception: pass` 改为 `except Exception: logger.debug(...)` 或至少添加注释说明为何可安全忽略。
6. **合并嵌套 with**（M-04）：4 处嵌套 `with` 可合并为单行多上下文，提升可读性。

### 长期

7. **引入 pre-commit hooks**：配置 ruff pre-commit，防止新问题进入 main 分支。
8. **添加 pyproject.toml 的 ruff 配置**：明确启用的规则集、排除目录和 per-file-ignores。
9. **CI 集成 ruff check**：在 GitHub Actions 中添加 ruff 检查步骤，新 PR 不允许引入新的 ruff 错误。

---

## 五、总结

项目整体质量良好，架构清晰，文档详尽，安全措施较为完善。主要改进空间在于：
- **代码整洁度**：1,444 个 ruff 告警（大部分已有 open PR 覆盖）
- **异常处理精度**：331 处裸异常捕获需逐步收窄
- **时区安全**：33 处 datetime 未绑定时区

现有 30 个 open PR 已覆盖了绝大多数严重和高优先级问题，建议优先合并后再处理本次新发现的 6 个中等问题。
